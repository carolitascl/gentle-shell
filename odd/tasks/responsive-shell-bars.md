# Responsive shell bars

## Objective and problem
Make Customize → Status: hidden remove the bottom status bar, and avoid showing both top and bottom bars at narrow/mobile widths.

## Authorized scope and expected behavior
- Hidden removes the status rail and bottom status bar at all widths, including regular mode.
- At narrow widths, preserve the configured top bar when `headerPlacement` is `top` (including the current default); otherwise show the bottom bar only. Do not show both at once.
- Use the top bar's more useful data as the content reference for the bottom-only path, including its usage/context details where available, while preserving extension statuses. Avoid an empty reserved footer row if feasible under the host layout contract.
- Update relevant UI preview/docs and regression tests. No remote operations or unrelated UI redesign.

## Constraints and route
- Two behavior work units; delegate a single scoped writer per unit because implementation and tests span multiple nontrivial files. One writer at a time.
- Test-first where deterministic layout tests allow observed RED → GREEN; run focused tests, typecheck, and applicable full checks at closure.
- Delivery strategy: ask-on-risk; forecast under approximately 400 authored changed lines, generated files excluded. Running committed authored lines: 327 through T2 (89 + 238). Committed slice boundary starts at `553cd776b384952e476e8d7bace9765566c88a10`; no PR requested.
- User explicitly authorized local work-unit commits and native review. Push, PR, and merge remain unauthorized.

## Checklist
- [x] T1: Make hidden suppress bottom bar (including regular and narrow rendering), update the hidden preview/docs and tests. Route: delegated; multi-file source/test write. Acceptance: no bottom paint with hidden; other placements still paint normally. Evidence: RED 7/40 tests failed before implementation; GREEN 40/40 layout tests pass. `git diff --check` passed. Typecheck has 188 existing errors on baseline and changed tree, no new errors reported. Committed `4e512d47778686d887f03af8ddb817d61a86c9a2` (`fix(shell): hide bottom status when placement is hidden`); exact staged tree focused 40/40 layout and 211/211 related tests passed. Native assessment for exact T1 initially failed while the feature document was untracked; no lineage was started.
- [x] T2: Resolve narrow duplicate bars according to the configured top-bar preference; align bottom-only data with the top bar's richer content while retaining extension statuses; update tests and docs. Route: delegated; multi-file source/test write. Acceptance: at narrow width, configured `top` shows top only; `below-input` shows bottom only with key top-bar content and extension statuses; wide layout unchanged. Evidence: RED layout/integration tests observed; GREEN layout 43/43, scroll 2/2. Full unit suite with isolated HOME: 3524 pass, 34 skipped, 0 fail of 3558; raw HOME suite has one unrelated global Vim preference failure. `pnpm -s typecheck` exit 0 with 188 recorded baseline diagnostics and no regressions. `git diff --check` passed. Committed `723bc1304d114967985a6baa2bd8637edf9f7347` (`fix(shell): show one rich status bar on narrow screens`); independent full suite and typecheck completed before commit. Native committed-slice review unavailable: assessment rejected its schema and inspect selected an unrelated older base; no lineage was started.

## Progress and next step
Exploration found current hidden behavior deliberately leaves the bottom bar visible and narrow layouts paint both bars. The user clarified top-preferred if explicitly selected, otherwise bottom, and supplied mobile screenshots showing that the top bar's content is preferable. T1 implementation is committed and checked. An unauthorized writer stash push/pop was investigated read-only: the original stash top remains intact, and parent `git status`, `git diff --stat`, and `git diff --check` show only the intended five edits and this task file. T2 implementation and independent checks are complete; `top` is the existing default value and the settings schema does not record whether a value was explicitly chosen, so the configured value controls ownership. The top-only path omits extension statuses by design; bottom-only retains them on a second line. Native assessment of the intended committed slice from `553cd776b384952e476e8d7bace9765566c88a10` returned `schema-incompatible`. Inspect then offered only a committed range from `c79f65d1a43e5b19e859d5127e32e62c753faa0d`, including unrelated historical paths. To avoid starting authority for the wrong candidate, no lineage was started or approved. Writer verification plus independent full-suite verification are recorded above; native review remains unavailable. Next: resolve the native selector/assessment mismatch before reviewing this exact slice, then decide any remote delivery separately.
