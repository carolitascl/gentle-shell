# Feature: customize-vim-mode

## Objective and authorization

Expose the existing global Gentle Vim prompt-editing preference inside `/gentle:customize`, as requested. Local implementation only: do not push, open a PR, or merge without separate authorization. Branch `feat/customize-vim-mode` starts at main merge commit `fb7afbf3056f34382d51ad559f6c121547650d9b` in an isolated worktree. Leave the concurrent Vim worktree unchanged.

## Problem, scope, and constraints

`/gentle:vim` already shows and changes the persisted global on/off preference, but the visual configuration panel omits it. Add an Editor category with an honest current state and explicit enable/disable controls, reusing the same policy store and live prompt update as the command. A compatibility rejection must not misleadingly claim Vim is active. Do not create another Vim setting, include Vim in visual profiles/reset, or alter the Vim editing engine. Preserve existing categories, installed-theme-only behavior, keyboard navigation, and narrow/short layout.

## Work unit

- [x] **V1 — Integrate Vim preference into the customization modal.** Route: delegated direct across extension, view, tests and README. Added an Editor category with explicit global Vim enable/disable controls, source/effective read-only preview, shared `/gentle:vim` status reporting and live prompt update only after successful persistence. No new store or profile/reset behavior. The writer's first test-only run stopped on missing Pi packages (1 pass, 3 module-load failures), **not** a behavioral RED; subsequent typecheck unexpectedly installed ignored local dependencies and ran postinstall, with no tracked side effects, and no further install was requested. A separate disposable base worktree then replayed the same test bytes **before** the same source bytes: 257 pass/4 behavior failures (missing `Vim: enable`), followed by 261/261 GREEN; file hashes matched the candidate. Independent verification found an unsupported causal claim when another session changed `vim.json`; a further RED regression failed once, then neutral saved-preference/effective-prompt wording passed 262/262, independently rechecked 262/262. `pnpm run typecheck`: 188 recorded baseline diagnostics, no regressions; `git diff --check`: clean. First full `pnpm test` after the wording correction had 3,429 pass, 1 `ENOTEMPTY` temporary-directory cleanup failure in a gentle-agents test and 34 platform skips; **one** bounded full rerun passed 3,429 with 34 skips, provider-contract/runtime-harness passed; no root cause is claimed for the first failure. Actual offline Pi 0.87.1 TUI in disposable HOME confirmed the framed Editor card at 160×42, 100×30 and 40×20, preference/effective off initially, enable persisting schema `gentle-pi.vim/v1` with on/on and INSERT prompt, then disable restoring off/off and ordinary editing. Evidence `/tmp/pi-customize-vim-smoke.GN2c6yJO/`. Vim motions and persistence across Pi restart were not live-tested. Native RDD remains clone-local off (`disabled/unmanaged`), so no native review was started. Work-unit commit `c7d9a70325fa8724bc39739989ce53de37d5b137` (189 authored changed lines/6 files including this record); local branch only.

## Verification and delivery

TDD: enabled per the existing visual-customization project/session configuration; focused runner `node --experimental-strip-types --test <test files>`, full runner `pnpm test`. Native RDD read-only status: global on, clone-local off, effective off; do not enable it or claim native approval. Risk-assess delegated changes and apply independent verification when required. No remote operation is authorized for this new request. Advisory authored-change forecast: roughly 100–200 lines; the 400-line heuristic is not a cap. Final state: focused and typecheck parent spot checks passed after the live TUI run; independent verification passed, the feature work unit was committed locally, and no remote operation occurred.
