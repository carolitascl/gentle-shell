# Fullscreen Scroll Cache

## Objective and problem
Make long-conversation scrolling in Gentle Shell fullscreen with the desktop sidebar responsive without changing frame output, geometry or pointer behavior. The settled viewport composition, not Markdown parsing, dominated the measured sidebar cost.

## Authorized scope and constraints
- Worktree `feat/fullscreen-scroll-tui-0871` from baseline `ac09a7ff8235c8059849eaf50ed127c6b5000186`. Preserve the dirty failed Segmenter-cache prototype in `feat/fullscreen-scroll-cache` and other original checkouts; do not stage it. Pi's proven local paint-reuse fix is committed only in its separate worktree (`638bb74db`, `05604433a`), not published.
- User authorized a local, isolated upgrade of Gentle Shell's `@earendil-works/pi-tui` and `@earendil-works/pi-coding-agent` together to cached `0.87.1`, followed by runtime and scroll compatibility checks. User separately authorized anonymous metadata reads and package downloads only from `registry.npmjs.org` for this paired upgrade and its necessary dependency tree. No credentials, publishing, other remote destination, GitHub operation or PR/merge is authorized. After the 3-day release-age gate refused the aligned stack and pnpm rejected duplicate old/new exclusions, user explicitly authorized **replacing** the six existing `@earendil-works/*@0.85.1` `minimumReleaseAgeExclude` entries with permanent exact `@0.87.1` entries for pi-coding-agent, pi-tui, chord, pi-agent-core, pi-ai and pi-telemetry. Do not exempt anything else, weaken the global age/trust policy, update unrelated packages, or ship a pnpm-only patch as an npm-consumer fix.
- Preserve frame bytes within a version, sidebar and narrow behavior, fullscreen geometry, theme, scroll and pointer semantics. Treat synthetic latency as diagnostic, not proof of the live freeze. Maintain tests/docs with behavior; make local Conventional Commit work units only after checks.
- TDD on, from `openspec/config.yaml` `strict_tdd: true`; focused `node --experimental-strip-types --test tests/shell-sidebar-scroll-benchmark.test.ts tests/shell-sidebar-fullscreen.test.ts` and full `pnpm test`. Do not invent RED for a dependency pin; record baseline characterization and real failures. First prefer offline installation; after the observed cache failure, only the specifically authorized anonymous public-registry fetch is permitted, with lifecycle scripts disabled.

## Plan and acceptance criteria
- [x] T1 — Characterize settled scroll and parity in native fullscreen sidebar on/off and width-matched controls. Route: delegated direct. Baseline and parity commits `8efb84042d2c93415323c8a2f33d8f001509eb92` and `ac09a7ff8235c8059849eaf50ed127c6b5000186` (249 authored lines together).
- [x] T2 — Align Pi TUI and coding-agent to 0.87.1 via pinned `package.json` and `pnpm-lock.yaml` with exactly six authorized permanent release-age replacements in `pnpm-workspace.yaml`; verify the runtime compositor resolves the intended TUI copy, not only Gentle Shell extensions. Route: delegated direct writer (multiple non-trivial files and install); preserve an exact offline install and run focused scroll/parity tests. If compatibility breaks, stop and report rather than papering over behavior or crossing scope. Commit a coherent work unit after proof.
- [ ] T3 — Correct 0.87.1 compatibility evidence and verify representative workloads and packed-consumer probes: update the manifest's exact dev pin and two development-test docs (retain the deliberate `>=0.85.1` peer floor); keep the cancellation test strict about rejection while accepting the observed Pi 0.87.1 `Command terminated without an exit code`; correct release-age comment provenance; restore the checksum-verified pinned Gentle AI binary locally for the harness without network. Then run typecheck, full `pnpm test` and packaging checks. Route: delegated writer for 2+ files plus independent verification.

## Evidence, limits and delivery
T1 Pi 0.85.1 synthetic settled 1200-row Markdown: 140-column sidebar median ~0.34 ms versus equal-content-width no-sidebar 0.024 ms, zero Markdown misses. Forced invalidation cost occurs in both. Tests asserted raw narrow-frame equality for all 15 sampled frames and matching desktop transcript anchors. An experimental Gentle-owned Segmenter cache cut native calls 503→182 but repeatedly regressed latency; uncommitted and excluded. Pi local previous-frame paint reuse measured ~1.7→1.23 ms at 140×24 and ~4.5→3.05 ms at 180×50 with 80 exact frame/terminal-hash parity records, but is unpublished.

Before this upgrade, a **local-only** temporary module resolver compared installed 0.85.1 to cached 0.87.1 using the clean Gentle Shell benchmark: both passed 2/2; 1200-row sidebar ANSI medians 0.607→0.318 ms, Markdown 0.681→0.467 ms, plain 0.243→0.264 ms. These are 15 synthetic frames per condition and parity was within, not across, versions. Cached 0.87.1 `layout.js` has an intra-frame fast path, not our previous-frame fix. Runtime resolution and user-session effect remain unverified.

The first isolated `pnpm install --offline --ignore-scripts --no-frozen-lockfile` was blocked by `ERR_PNPM_NO_MATCHING_VERSION` for `pi-tui@0.87.1`; the writer restored both pins to 0.85.1 and changed no source, test or lockfile. npm's local cache independently contains integrity metadata and tarballs for both 0.87.1 packages, but pnpm's offline resolver cannot see them. The user subsequently authorized anonymous public-registry metadata and package downloads from `registry.npmjs.org` for this upgrade only; the first registry-restricted install resolved TUI 0.87.1 but failed `ERR_PNPM_NO_MATURE_MATCHING_VERSION` for coding-agent 0.87.1 (published 2026-09-22, two days old vs the three-day rule). User then authorized permanent version-specific exclusions for the two direct 0.87.1 packages only. A bounded writer found that pnpm 11.1.1 rejects duplicate version-specific exclusions for the same package when 0.85.1 and 0.87.1 entries coexist, and that coding-agent 0.87.1 requires chord, pi-agent-core, pi-ai and pi-telemetry 0.87.1 transitively (all potentially blocked by the same age gate). The attempt restored every package/config/lock change; only this task document is modified. User separately authorized replacing all six prior 0.85.1 entries with exact permanent 0.87.1 entries for the aligned Pi stack. This replacement is now within T2 scope; the global age/trust policy remains unchanged and no further exemptions are implied. Keep other destinations, GitHub and publishing out of scope.

Committed T2 candidate `a5cc9b9d` changed package pins, six exact version-specific release-age exclusions, lockfile and benchmark `scrollToEnd()` API use. Scoped tests 4/4 and typecheck passed. T2 is complete on this scoped proof and the subsequent independent full unit/provider/harness checks recorded under T3; the packed-consumer proof remains pending under T3. Independent `pnpm test` FAILED: manifest expects old dev pin; cancellation fixture expected only `/aborted/` but Pi 0.87.1 throws `Command terminated without an exit code` on signal-terminated shell; harness lacked the pinned local Gentle AI v3.7.0 binary after install with scripts disabled. Provider-contract, runtime-module check and package-file check passed. The release-age comment incorrectly credits prior PR #634 with this new 0.87.1 authorization; fix before PR. No PR while required checks fail. User requested one sizeException PR only IF everything works; remote GitHub credential/session and ordinary issue/CI gates are still unconfirmed. Forecast this upgrade work unit at <200 authored lines plus lockfile churn; ~400 lines is advisory, not a reason to omit proof. Delivery strategy `single-pr` with user-selected size exception for the eventual Gentle Shell PR; push, PR creation and merge require separate exact destination, operation and credential/session authorization plus ordinary repository gates. The prior review boundary is `ac09a7ff`; assess each committed work unit under the user-owned RDD switch, not TODO checkboxes. Engram mirror topic `odd/fullscreen-scroll-cache/tasks` must contain this full document and locator; if unavailable, mark mirror pending.

T3 evidence: updated `tests/package-manifest.test.ts` dev pin to `0.87.1` (peer floor stays `>=0.85.1`); corrected the two "development tests pin 0.85.1" doc sentences in `docs/readme-reference.md` and `docs/gentle-shell.md` to `0.87.1`; removed the false PR #634 attribution from the `pnpm-workspace.yaml` release-age comment without inventing a replacement PR number. Re-investigated the cancellation fixture: the real abort-signal path (`sdd-managed-runtime-settlement.test.ts` "stock local shell wrapper ... preserves cancellation") already passes unmodified against Pi 0.87.1 because `bash.js` still throws `aborted`/`Command aborted` before checking exit code when the signal is aborted, matching `/aborted/`. The actual RED was a separate, non-abort case in "remediation shell captures numeric exit, preserves stock errors and executes once": Pi 0.87.1's stock `createBashToolDefinition` now rejects any `exitCode: null` (signal-killed convention) with `Command terminated without an exit code`, where 0.85.1 previously resolved. Updated that assertion to require the new rejection for the null-exitCode case only, keeping the numeric-exit and abort-signal assertions strict and unchanged; no production wrapper code touched. Restored `.gentle-ai/v3.7.0/{gentle-ai,integrity.json}` (gitignored) from the separate `fullscreen-scroll-integration` worktree only after independently recomputing the binary's SHA-256 and confirming it matches both that worktree's `integrity.json` and this repository's pinned `GENTLE_AI_RELEASE_ASSETS["darwin/arm64"]` digest in `scripts/gentle-ai-installer.mjs`; files copied (not symlinked) with `cp -p`, no network or source-worktree mutation. Verification: focused `node --experimental-strip-types --test tests/package-manifest.test.ts tests/sdd-managed-runtime-settlement.test.ts` 58/58 pass; `pnpm run typecheck` — 195 recorded diagnostics, no regressions; `git diff --check` clean; offline `pnpm test` (unit-tests 3400/3400 pass, 38 skip; provider-contract pass; runtime-harness pass); `pnpm run check:runtime-modules` and `node scripts/verify-package-files.mjs` both pass (171 files, 69 byte-pinned contract artifacts). Full unit/provider-contract/runtime-harness checks passed and work-unit commit `985fcb91` recorded. A subsequent independent packed-consumer preflight found four `scripts/test-packed-runner.mjs` modes hardcoded to SDK 0.85.1; `--unhooked-imports` fails at `project-sdk-version` before pack/install. Reopen T3 until these probe expectations are aligned to the pinned 0.87.1 SDK, the unhooked consumer probe passes under the authorized anonymous npm registry scope, and the changed script has independent verification. The four exact SDK-version guards in `scripts/test-packed-runner.mjs` are now aligned to 0.87.1 without relaxing the installed-SDK or packed-asset assertions. The unhooked consumer check is BLOCKED after two bounded, network-disabled attempts with an offline npm wrapper and local npm cache: the first passed pack but failed `install-command` with sanitized nonzero exit; a final diagnostic run passed pack and install but failed at `artifact-check` / `jiti-manifest-owned`. The runner cleans its temporary consumer, so the exact Jiti resolution error is not yet proven; no further blind retry. The four 0.87.1 guards and this document remain uncommitted. The default packed mode needs an unapproved GitHub release download and remains explicitly unverified. Do not claim all packaging gates passed or open a PR while the user's all-checks condition is unmet. User authorized one bounded offline diagnostic preserving a disposable consumer. Manual pack and install each succeeded; the SDK's jiti@2.7.0, package manifest and static export are present as regular files. The precise failure is lexical ownership comparison: on macOS `/tmp` is a symlink to `/private/tmp`, while `createRequire.resolve('jiti/package.json')` returns the canonical `/private/tmp` path. `assertOwnedRegularFile(consumerDirectory, relative(consumerDirectory, jitiManifest))` misclassifies it as escaping the owned `/tmp` root. Scoped correction implemented (uncommitted): `resolveInstalledJitiStaticEntry` now derives the resolved Jiti manifest's relative path from `realpathSync.native(consumerDirectory)` but passes it to `assertOwnedRegularFile` with the original lexical consumer root. New `tests/packed-runner-owned-path.test.ts` exercises an ancestor symlink alias and rejection of a symlink at the owned Jiti static entry. Strict TDD RED: 0/2 tests passed before the path correction (both stopped at `path escapes owned root`); GREEN: 2/2 passed after it. Existing manifest and managed runtime settlement tests passed 58/58; `git diff --check` clean. One independent offline packed probe then passed pack, install and Jiti ownership, but FAILED at `import-probe-command` (child exit 1; typed receipt hides exact cause). No retry was made at this new stage, and focused/typecheck/full-suite checks were not rerun after this fix. T3 remains blocked and the four SDK guard changes, canonical ownership fix, focused test and task document remain uncommitted. Default packed mode separately requires an unauthorized GitHub release download and remains unverified. No PR while the user's all-checks condition is unmet. User authorized one scoped offline import-probe diagnosis. A first preflight failed before any pack/install due to the chosen wrapper's stderr destination and npm config collision; a corrected existing offline wrapper with isolated user config then passed preflight. One manual pack/install and one inert import probe found the first cause: `MODULE_NOT_FOUND` for `@earendil-works/pi-ai/compat` while importing `lib/review-host-relay.ts` from packed `gentle-ai.ts`. The installed coding-agent has pi-ai nested under itself, while Gentle Shell's package.json declares pi-tui and pi-pretty but not the pi-ai module used at runtime. This is a candidate packaging dependency failure, not a cache miss. Scoped dependency correction in progress: added a manifest test requiring direct exact pi-ai@0.87.1, observed RED (50/51 pass; dependency undefined), then added the runtime pin to `package.json` and observed GREEN (51/51 pass). Offline `pnpm install --offline --ignore-scripts --lockfile-only` failed `ERR_PNPM_NO_MATCHING_VERSION` for pi-ai@0.87.1: pnpm's offline metadata reports latest 0.85.1 despite pi-ai@0.87.1 already appearing transitively in the existing lockfile. No online fallback or manual lockfile edit was attempted; `pnpm-lock.yaml` remains unchanged and is out of sync with the new direct pin. Additional focused tests passed 10/10; `pnpm run typecheck` failed before typechecking when pnpm attempted a module-directory purge without TTY. With the user's existing anonymous `registry.npmjs.org` authorization reaffirmed, one bounded `pnpm install --lockfile-only --ignore-scripts --registry=https://registry.npmjs.org/` succeeded under a stripped environment with `/dev/null` user/global npm config. The only lockfile change is the root importer dependency on pi-ai `specifier: 0.87.1`, `version: 0.87.1(ws@8.21.3)`; no other versions or policy changed. Direct `node scripts/check-types.mjs` passed (195 recorded diagnostics, no regressions; four file/code pairs improved) after the prior `pnpm run typecheck` precheck refused a module purge without TTY. Manifest tests passed again (51/51); `git diff --check` clean. An independent offline `--unhooked-imports` probe after that dependency correction verified pack but FAILED at `install-command` (child exit 1; typed receipt hides npm cause; install incomplete, runner cleanup complete). It was run once and not retried; later checks and full unit/provider/harness were not rerun on this candidate. A read-only npm cache listing shows pi-ai@0.87.1 metadata and tarball entries, but does not prove the install failure's cause. User authorized one bounded offline preserved-consumer install diagnosis. A manual pack (one validated tarball) and install (same command and SDK version) both SUCCEEDED under `env -i` with the shared offline npm cache; no import probe or packed runner was rerun. This does NOT explain the runner's preceding typed `install-command` failure: its `isolatedUnhookedEnvironment` provisions a separate temporary cache and other isolated variables that the manual diagnostic did not reproduce exactly. Do not call that prior failure a cache miss or claim consumer proof. User authorized one reproduction matching the runner isolation. Child preflight observed offline=true and effective cache inside its isolated temporary root (not the shared cache), then one npm pack succeeded. The diagnostic's own tarball containment check incorrectly rejected npm's canonical `/private/tmp` path against its lexical `/tmp` pack directory; no install or import probe ran, and owned temp was cleaned. This is a diagnostic false negative, not a new package failure. Do not retry under the spent one-run authorization. User authorized one corrected offline reproduction. The diagnostic first validated canonical containment with a dummy file and a rejected sibling, then proved child npm offline=true and effective cache inside the disposable isolated root (not the shared cache). One verified pack succeeded and one install under the runner-equivalent environment failed `ENOTCACHED`: npm had no cached response for the public Pi SDK request. The isolated cache is freshly created per run; this explains the offline runner install failure and does not implicate the pi-ai runtime pin. A `tmp` config warning was secondary. No runner or import probe was retried, and owned temp was removed. A prospective online unhooked probe was stopped at preflight before any pack/install: the approved local wrapper configured `registry.npmjs.org` and `ignore-scripts=true`, but could not enforce that all actual dependency requests would stay on the approved host. This is a remote-destination safety limit, not evidence that code failed. No GitHub/network request occurred. User selected the limited offline shared-cache import check. A temporary wrapper explicitly overrode BOTH npm cache setting variants to the populated shared cache while forcing offline=true and ignore-scripts=true; preflight confirmed the effective cache and offline mode despite the runner's conflicting isolated-cache settings. Exactly one `node scripts/test-packed-runner.mjs --unhooked-imports` returned `status: complete`, `checkId: unhooked-imports-complete`, with packVerified/installCompleted/cleanupCompleted all true. Focused manifest+owned-path tests passed 53/53 and `git diff --check` passed. This is meaningful evidence that the packed candidate imports under a populated offline cache, but deliberately DOES NOT certify the normal fresh isolated-cache gate or default postinstall/GitHub mode. A fresh full local verification on the current uncommitted candidate now passed: 3,403/3,441 unit tests passed, 38 skipped; provider-contract 1.2.0, runtime harness, runtime-module check (7), package-file check (171), typecheck with no regressions against 195 recorded diagnostics, and `git diff --check` all passed. Real scrolling latency and normal fresh-isolated/default packed gates remain unverified. The user explicitly granted a PR-only exception with pending checks documented, but NOT merge until issue approval, CI and pending verification resolve; authorized the active GitHub.com `gh` session for API and HTTPS push to `Gentleman-Programming/gentle-shell`, not SSH. GitHub session was verified, base branch is main. Issue search found no confirmed approved issue matching this Pi compositor/dependency work: #832 is approved but closed and concerns a different sidebar invalidation fix, while #1254 has no approved label; do not mislink either. The user authorized creation of a new issue after confirming no existing approved issue number. GitHub issue #1424 (`https://github.com/Gentleman-Programming/gentle-shell/issues/1424`) was created once and confirmed by exact target-host readback of number, URL, title, body and labels. It records the requested all-user investigation scope without claiming cross-platform reproduction; macOS and gentle-pi 3.7.0 / Pi 0.85.1 describe the verified local source/benchmark baseline, not the unknown original live installation. The user then directly authorized approval of exact issue #1424 in `Gentleman-Programming/gentle-shell`. A fresh target-host read confirmed the issue was OPEN with `bug` and `status:needs-review`, and the authorized `gh` actor had ADMIN permission. One atomic issue update replaced only `status:needs-review` with existing `status:approved` while preserving `bug`; exact target-host readback confirmed OPEN issue #1424 and labels `bug`, `status:approved`. The issue-first condition for opening a PR is now met. User requested PR and merge, but their prior exception covered opening the PR with pending checks only; merge remains blocked until all pending checks and CI pass. Current branch is four commits ahead of the common ancestor while remote `main` is 46 commits ahead (read via authorized HTTPS/gh session without changing branch or FETCH_HEAD); `package.json` and `tests/package-manifest.test.ts` changed on both sides, and a read-only merge-tree reports overlap. Reconcile before claiming PR mergeability; never overwrite remote changes or rebase destructively without authorization. RDD effective on globally for this clone; native inspect found no previous lineages and offered a committed PR-slice review from the branch point. Native assess over the 11-path, 1,009-line committed range returned high risk; independent verifier already ran the full local suite, while native review remains pending until the final candidate is settled. The packed runtime correction and its tests were committed as work unit `42e36ad535ac86a710781251526538287ee3b781` (`fix(deps): verify packed Pi 0.87.1 imports`) after the full local suite passed; a passive evidence update followed in `8c367bef6cce1f16b50fe684abef9ac29944e3e9`. Draft PR #1426 was opened and read back at that head with approved issue #1424, one `type:bug` label, and all pending proof disclosed; the user directly authorized and target-host readback confirmed `size:exception` on that exact PR. The user also authorized integrating remote `main` 8035114a57 (46 commits ahead) and accepting upstream deletion of the retired SDD test, but NOT merging PR to main while gates remain. A local `git merge --no-commit --no-ff` is IN PROGRESS; only conflict was the deleted `tests/sdd-managed-runtime-settlement.test.ts`, resolved by the explicitly authorized removal. Effective staged PR diff vs remote main is 10 paths / ~1,004 authored changed lines. Independent post-merge unit verification FAILED: 3,397 passed, 50 failed, 34 skipped. Read-only mapping attributed 47 failures to stale 0.85.1 Vim editor test/version fixtures against installed 0.87.1, two to scroll benchmark narrow/sidebar-off frame assumptions changed by main, and one to `gentle-agents` temporary cleanup `ENOTEMPTY` of unknown causality. No other full checks ran after this failure, and the merge is not committed or pushed. Next: scoped test compatibility correction that preserves the strict production editor guard and exact frame parity, then one independent full check; diagnose ENOTEMPTY separately if it recurs. Do not claim current candidate is green or merge PR. Engram mirror update pending parent reconciliation.

Post-merge scoped correction (uncommitted; merge/index preserved): aligned installed-pair Vim fixture versions, PATH/local identity assertions and explicit genuine 0.85.1 mismatch rejection to the declared 0.87.1 pair; the production adapter guard is unchanged. The narrow sidebar fallback now retains the full-width header on current main, so the benchmark's no-rail narrow control installs the same fallback header/layout hook while keeping the footer as a plain dock component. All raw narrow frames remain asserted byte-for-byte equal, including invalidated ANSI/Markdown frames; desktop sidebar-vs-no-sidebar transcript and width-matched comparisons remain strict. The parent-provided prior full suite is RED (3,397 pass / 50 fail / 34 skip), including 47 stale Vim fixture failures, two narrow-frame failures and one unrelated cleanup ENOTEMPTY. The human selected Strict TDD for this correction only, with the focused Node runner; no global setting is inferred. Focused GREEN: `node --experimental-strip-types --test tests/vim-editor-adapter.test.ts tests/gentle-shell.test.ts tests/shell-sidebar-scroll-benchmark.test.ts` passed 238/238, zero failed/skipped. An independent post-correction full run passed 3,446/3,480 tests (34 skipped, zero failed); provider contract, runtime harness, typecheck (188 recorded diagnostics, no regressions), runtime-module check, package-file check (152 files) and both diff checks passed. The single earlier ENOTEMPTY cleanup failure did not recur; its cause remains unknown, not proven to be a flake. Fresh isolated/default packed consumer gates and real-session scrolling remain pending. The merge of main is staged locally but uncommitted and unpushed; no merge-to-main authorization follows from this proof. Engram mirror reconciliation remains pending parent ownership.
