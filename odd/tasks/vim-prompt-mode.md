# Vim prompt mode

## Objective
Add an opt-in `/gentle:vim` preference for Gentle Shell's prompt, following `/gentle:animations` for global persistence, status, interactive selection, and live updates. The editing semantics must match the documented Claude Code Vim editor mode, not merely a Vim-like h/j/k/l subset. Ordinary editing remains the default.

## Problem / why
Users accustomed to Vim need modal prompt navigation without replacing the existing Gentle prompt or disturbing Pi's other editor owners. Escape currently has turn-cancellation and idle-draft semantics, so mode transitions must not accidentally trigger those effects.

## Authorized scope
Local feature implementation in `lib/vim-*.ts` and `tests/vim-*.test.ts` (new headless editing engine), `extensions/gentle-shell.ts`, `lib/command-palette-catalog.ts`, `tests/gentle-shell.test.ts`, `tests/command-palette.test.ts`, and `docs/readme-reference.md`. This feature document is `odd/tasks/vim-prompt-mode.md`. Preserve the unrelated untracked `odd/tasks/visual-customization.md`. PR feedback is separately authorized for `Gentleman-Programming/gentle-shell` #1317, #806, #1390–#1395 using the user's active `gh` session (posted separately).

## Constraints and decisions
- Off by default; enabling is explicit. Follow animation policy's global persistent file and status/menu shape, including headless status fallback, invalid policy fallback, and immediate live application.
- Only the Gentle-owned prompt uses modal editing. User explicitly authorized a larger owned input-editor redesign after the Pi 0.85.1 visual-selection proof failed. Preserve the existing Gentle frame, avoid replacing a foreign editor, and do not modify Pi core.
- Source of truth: https://code.claude.com/docs/en/interactive-mode#vim-editor-mode (public docs fetched directly). Match its documented prompt command families: INSERT/NORMAL/VISUAL character/line modes; `i/I/a/A/o/O`, `h/j/k/l`, Space, `w/e/b`, `0/^/$`, `gg/G`, `f/F/t/T` and `;/,`; keep `/` for Pi commands and skills even in NORMAL; reverse prompt-history search is excluded by explicit user choice; edit `x`, `d/c/y` line/word/find motions, `s/S`, `p/P`, indent/dedent, `J`, `u`, `.`, text objects (word/WORD/quotes/brackets), visual operators, and `Ctrl+[` where Pi receives it. Block-wise visual is explicitly not supported by Claude Code. Do not advertise features not actually implemented.
- Explicit user choice: in INSERT, Escape only enters NORMAL, never cancels a running turn or clears a draft; subsequent Escape in NORMAL follows existing Gentle Escape semantics. This is a Gentle override of Claude Code's otherwise modal Escape, and must be visible in docs. Preserve autocomplete, queue/abort, bash-mode, empty-draft and double-Esc behavior with tests.
- Use Pi's existing editor owner/shortcut/autocomplete contracts. No silent NORMAL writes from encoded keys, paste, autocomplete or shell-mode. Preserve cursor, undo and any paste markers. Pi 0.85.1 public editor API cannot set cursor, edit a range atomically or render visual selection. Before expanding commands, prove a narrow version-gated private adapter in isolation with tests; fail closed on unknown layout. Do not mutate private state without that proof.
- Existing MIT pi-vim reference (https://github.com/burneikis/pi-vim) implements many families but targets `@mariozechner/pi-*`, replaces the editor factory, and accesses private state. The MIT `@0xkahi/pi-vim-keys` 1.1.2 reference (https://github.com/0xKahi/pi-vim-keys) highlights visual selections by overlaying visible wrapped rows after `super.render`, capturing the scroll offset from `renderTopBorder`; its private-state/word-wrap dependencies have drift tests. Its dev dependencies target Pi 0.87, so behavior against our installed Pi 0.85.1 is unproven. Study its design/tests, but preserve the Gentle editor factory, live toggle, frame, and Esc semantics; do not install either package as another editor owner or copy code without MIT attribution.
- Artifacts, comments, and docs in English. User explicitly requested PR and merge after completion; work-unit commits and the necessary push are delivery prerequisites, not permission to merge unverified or unrelated changes. Do not include the unrelated task file.
- Advisory task sizing: ~400 authored changed lines per work unit is a heuristic, not a hard acceptance limit. Do not omit tests or compress code to fit it.

## Route, tests, delivery
- Delegated direct: broad editor work touches multiple non-trivial files; one bounded writer per sequential work unit, with allowed edit surfaces and foreground tests. No parallel writers in this worktree.
- TDD: strict, source `openspec/config.yaml` project setting and prior ODD convention; exact runner `pnpm test`, focused runner `node --experimental-strip-types --test tests/vim-*.test.ts tests/gentle-shell.test.ts tests/command-palette.test.ts`. Record actual RED and GREEN; type check `pnpm typecheck`.
- Branch `feat/vim-prompt-mode` from `main` commit `4d702a47`; no tracked source changes preceded this feature. Delivery strategy `single-pr` with explicit `size:exception`, chosen by the user after the >1,000 authored-line forecast; keep reviewable work-unit commits but open one PR only after complete verification. User asked for PR and merge after verification without waiting for optional CodeRabbit. Issue #1413 was created and labeled `status:approved` with explicit user authorization. RDD native review follows candidate consent and no delivery inference. Work-unit commit `442e7db0c652c163885c42f16097365c4d647df9` contains the gated modal engines with their tests (2,103 authored lines); no unrelated files.

## Tasks
- [x] VIM-0: Gentle's owned editor uses a post-render visual overlay. Headless actual-editor tests cover SGR, cursor, scrolling, markers, autocomplete and frame on local Pi 0.85.1; class-bound 0.87.1 unbundled/bundle fixtures also pass. The user confirmed live VISUAL change/paste after the stale-session correction. Commits `442e7db0` and `8e0e8595` carry the engine and integration with tests.
- [x] VIM-1: Strict preference/menu, live toggle, shortcut priority, queue, NORMAL-input blocking and two-stage Escape have regression tests. Independent bash Esc, history navigation and shortcut-exception findings were corrected; verified by `pnpm test` and independent spot-check, commits `442e7db0` and `8e0e8595`.
- [x] VIM-2: Grapheme/multiline NORMAL motions, insert/open, counted find and repeat work in the gated adapter. Pi's `/` commands and skills are routed through native completion in NORMAL (tested headlessly; live slash completion not separately observed). Reverse prompt-history search is excluded by user decision. Commits `442e7db0` and `8e0e8595`; `pnpm test` passed.
- [x] VIM-3: Operators, text objects, registers, paste, J, linewise/motion indent, scoped `.` replay of normal and completed insert/change text, and insert-session undo have headless tests. Independent undo, shift endpoint and literal-tab indent findings were corrected; `pnpm test` passed; commits `442e7db0` and `8e0e8595`.
- [x] VIM-4: Character/line visual selections, text objects and documented visual actions including paste and case changes have tests. Independent EOF, empty register, pending-operator, tab-dedent and leaked insert-session defects were corrected. The user confirmed live c/p; `pnpm test` passed; commits `442e7db0` and `8e0e8595`.
- [x] VIM-5: Palette/docs describe supported commands, Pi slash/menu and two-stage Escape divergences, compatibility fallback, and unsupported behaviors. Independent final review and focused/full/typecheck/structural checks passed; commit `8e0e8595` includes docs, palette and tests.
- [ ] VIM-6: Once verified, create reviewable work-unit commits under user's `single-pr` `size:exception`, run native review only when RDD requires it, open one PR, observe CI, then merge as explicitly requested.

## Acceptance criteria
- `/gentle:vim` offers a selector in interactive mode and status headlessly; `enable`, `disable`, and `status` report effective mode and persist as animation policy does.
- Existing prompt instances switch immediately; new sessions load the persisted choice. Disabled mode preserves all pre-existing editing behavior.
- Documented Claude Code Vim command families above work on Unicode/multiline prompts, including visual selection/operators. `/` continues to open Pi commands and skills, including from NORMAL, by explicit user choice; reverse prompt-history search is omitted and documented as a deliberate divergence. Other unsupported commands are reported honestly. Escape follows the user's explicit two-stage Gentle decision without discarding queued text.
- No text-producing NORMAL-mode path, app shortcut, autocomplete or shell-mode silently changes the draft. Owned-editor guard, tests and typecheck remain intact.

## Progress / evidence
2026-09-24: Initial minimal writer produced a provisional opt-in mode (focus 143 pass; full suite 3404 pass/38 skipped; typecheck baseline 195 diagnostics, no regression), but independent verification found unsafe NORMAL input, app shortcut and autocomplete/bash paths and insufficient command parity. The user explicitly changed scope to Claude Code Vim parity, then asked to finish all work. The in-flight minimal correction was canceled before delivery. GitHub comments were published for the authorized contribution PRs as a separate workstream.

## Progress / evidence (continued)
2026-09-25: PR #1415 CI `verify` job failed deterministically in two test files while local `pnpm test` passed, because CI runs the synthetic `pull_request` merge of the feature branch into an already-advanced `origin/main`, not the isolated feature-branch tree.
- **`tests/command-palette.test.ts` — RED (CI only), root cause proven, GREEN confirmed.** `origin/main` independently added `import { existsSync, readFileSync } from "node:fs";` (for its own retired-SDD-commands test) at the same import block the feature branch had extended with its own separate `import { readFileSync } from "node:fs";`. A `git merge-file` 3-way simulation against the actual merge base confirmed this combination silently produces two declarations of `readFileSync` (exit 0, no conflict markers, still a duplicate-declaration failure at load time) — the literal CI symptom. Fix: the feature import now matches `origin/main`'s exact position and members (`import { existsSync, readFileSync } from "node:fs";` immediately after `import test from "node:test";`), so the merge is byte-identical on that line and produces one import. Re-simulated the merge with the corrected file: exit 0, single `readFileSync` declaration, and `main`'s "retired SDD commands are absent..." test is preserved verbatim in the merged output, diffed against `origin/main` to confirm only the two new Vim tests and the `gentle:vim` catalog entry are added. `existsSync` is intentionally unused on this branch alone (used only after merge, by main's test); `tsconfig.json` has `noUnusedLocals` off, so this does not fail `pnpm typecheck` locally, and `pnpm typecheck` confirms no new diagnostics.
- **`tests/vim-editor-adapter.test.ts` — RED (CI only), root cause proven, GREEN confirmed.** The file's module-level Pi discovery required an installed PATH `pi` matching bundled version `0.87.1` and threw at import time otherwise, failing the entire file. `package.json` declares only `@earendil-works/pi-coding-agent`/`pi-tui` `0.85.1`; Linux CI installs only that version, so no PATH `pi` resolves to `0.87.1` there (this only matches an incidental local dev-machine install, not a declared dependency). Fix: the discovery no longer throws; it sets `target`/`skip0871` and leaves the seven tests that need the bundled 0.87.1 fixture (`PATH bundled virtual host...`, both `bundled 0.87.1...` tests, `runtime identity resolves only the matching installed coding-agent/TUI pair`, `installed Pi 0.87.1 pair...`, `0.87.1 CustomEditor subclass...`, `runtime identity and prototype mismatch...`) using `test(name, { skip: skip0871 }, fn)` with an explicit reason string. The one test that previously asserted both 0.85.1 and 0.87.1 behavior in one body (`installed Pi pairs prove version...`) was split into an always-run 0.85.1 test and a skippable 0.87.1 test sharing one assertion helper, so 0.85.1/non-bundle coverage never depends on 0.87.1 availability. Verified by running the focused command with PATH restricted to only the Node binary directory (no `pi` launcher reachable): 7 tests report `SKIP` with the exact `skip0871` reason ("installed PATH Pi 0.87.1 bundle unavailable; only the declared 0.85.1 dev dependency is guaranteed in CI"), 0 fail, 29 other tests in the file still pass, including the new always-run default `resolveVimRuntime()` identity check against the declared 0.85.1 installation — matching what CI's own 0.85.1-only install will observe, with no false GREEN and no network install.
- One follow-up TS2345 diagnostic from the 0.85.1/0.87.1 split (`typeof CustomEditor` not assignable to `typeof Editor`) was fixed by widening the shared helper's optional custom-class parameter to `{ prototype: unknown } | undefined`; `pnpm typecheck` then reported 195 recorded diagnostics, no regressions (4 pairs improved).
- Final checks observed on this fix: `node --experimental-strip-types --test tests/command-palette.test.ts tests/vim-editor-adapter.test.ts` — 70/70 passed (0 skipped locally, since this machine has PATH Pi 0.87.1); `pnpm typecheck` — 195 recorded diagnostics, no regressions; `pnpm test` — all stages passed, 3543 passed, 38 skipped (pre-existing, unrelated platform skips), 0 failed.
- Risk carried forward: this fix only proves the import merges cleanly via an offline 3-way simulation against `origin/main`, not by triggering GitHub's actual synthetic PR merge; a real push/PR resync is still needed to observe CI green directly. The seven 0.87.1-gated tests will report `skipped` (not `passed`) on CI's own 0.85.1-only Linux runner — that is the intended, honest outcome, not a regression to chase.

## Next step
The user's second live warning revealed `[beginInsertSession:session-active]` on VISUAL `c`. A RED regression identified an unhandled extension-shortcut probe that entered temporary INSERT then restored NORMAL/VISUAL without ending its adapter session; `c` correctly refused the stale session. TDD fixes now close temporary sessions, including a throwing callback and failed visual replacement, and remove a failed endInsertSession entry before marker validation without relaxing safety guards. Focused 214 passed, full suite 3540 passed/38 skipped, typecheck no regression to 195 baseline diagnostics; independent verifier spot-checked the fix. The user subsequently confirmed live c/p behavior. Final independent review identified a delivery blocker: a shortcut callback that writes the draft then throws restored stale NORMAL/VISUAL selection. Strict TDD adds NORMAL/VISUAL write-then-throw regressions (RED observed), and the correction retains INSERT ownership on changed drafts while preserving original mode restoration and session cleanup on unchanged drafts (GREEN observed). Final checks observed: focused 215 passed; `pnpm typecheck` reports 195 recorded diagnostics and no regressions; `pnpm test` passed all stages (3541 passed, 38 skipped). Preserve unrelated `skills/branch-pr/SKILL.md` and `odd/tasks/visual-customization.md`. Delivery remains pending: PR #1415 CI rerun and merge; issue #1413 is approved. The native committed-range START rejected with `candidate-target-projection-drift` before creating a lineage; the user explicitly disabled RDD for this clone, leaving global mode on. Delivery is unmanaged by RDD. An occurrence with user-reported Claude Bridge context was added to existing issue #1192. Work-unit commits `442e7db0c652c163885c42f16097365c4d647df9` (engines/tests) and `8e0e8595e4ecf332aea96d08a76adc010e53b2ee` (integration/docs/tests) are recorded. Native assessment marked the committed range due/high but could not derive risk detail until unrelated untracked files are explicitly excluded; do not include those paths. Optional CodeRabbit is not a merge wait condition. **CI blocker on PR #1415, now fixed locally and pending push:** two portability failures in `tests/command-palette.test.ts` (duplicate `readFileSync` import on synthetic merge with `origin/main`) and `tests/vim-editor-adapter.test.ts` (hard dependency on an undeclared PATH Pi 0.87.1, unavailable on Linux CI) blocked the `verify` job; see "Progress / evidence (continued)" above for root cause, fix and exact checks. Delivery next step: commit these two test-portability fixes as their own work unit, push, and re-observe CI on PR #1415 before any merge.
