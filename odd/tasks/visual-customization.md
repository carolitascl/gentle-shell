# Feature: visual-customization

## Objective and authorization

Implement `/gentle:customize` as a unified keyboard-accessible visual settings overlay for Gentle Shell. The user explicitly authorized implementation and clarified that “top tab” means the Gentle Shell fullscreen header, which should move below the input. Do not add adjustable width/breakpoint or accessibility controls (contrast/reduced motion). The user authorized issue, PR, and merge operations on the verified `Gentleman-Programming/gentle-shell` origin using the configured `gh` session, subject to repository policy.

## Problem and rationale

Animation and banner preferences are split across commands; themes require Pi settings or manual JSON; desktop status/header layout is fixed. A unified panel should expose the existing controls and new appearance/layout preferences without duplicating or silently overwriting their stores. A live screenshot showed the first implementation as an ugly full-screen flat 31-row list without groups or a visible preview. The user selected a compact panel with category navigation on the left and controls/modal-only preview on the right, adapting to one category per screen on narrow terminals. A second live screenshot showed the compact frameless text panel still wastes space and lacks the visual language of Commands; the user explicitly requested the same style as Commands. A third screenshot showed the theme editor still frameless and an invalid-HEX error while editing an alias-backed role. The user then requested removal of the entire custom-theme editor, including creation, editing, import and export, while keeping everything else.

## Scope and constraints

- Group existing `/gentle:animations` policies (quality/performance/potato) and banner visibility/color under the panel; preserve existing commands and persistence.
- Select installed Pi themes and preview their source palettes inside the modal without applying them; selecting an installed theme explicitly remains available. The user removed the custom-theme editor and its create/edit/import/export actions after live testing; do not expose them in the UI or leave reachable write paths.
- Configure status presentation: right, bottom, or hidden; preserve responsive default (desktop >=140 columns, narrow/regular bottom). Move the Gentle Shell fullscreen header below the input if technically supportable without breaking renderer ownership; do not move an external terminal tab.
- Configure placement of the existing top UI/header under the input, density (comfortable/compact/minimal), visibility of Changes, Agents, TODO, usage/cost and model details, reset defaults, and named visual profiles for whole-setting combinations.
- Avoid direct terminal screen/scrollback writes; use Pi-supported APIs where available and isolate any host-internal layout adaptation, testing resize, narrow mode and restoration. Confirm Pi runtime compatibility before assuming live theme APIs.
- Technical artifacts and UI copy default to English. Local implementation only. Preserve existing work and baseline compatibility.

## Configuration and delivery

TDD: enabled from `openspec/config.yaml` (`strict_tdd: true`); exact full runner `pnpm test`, focused runner `node --experimental-strip-types --test <test files>`. Require observed RED/GREEN/REFACTOR for new behavior. Review mode: on globally at feature start, recheck candidate-scoped status before any native operation; never enable it on user's behalf. Delivery strategy: ask-on-risk; user selected stacked-to-main chained PRs (no PR created yet). Forecast: >400 authored changed lines overall across multiple coherent work units (rough estimate 900–1800, to refine). Running authored changes: approximately 3734 lines including a 30-line focused crash correction, a 357-line compact-panel work unit, and a 922-line Commands-style/removal work unit across T1 (213), T2 (400), T3 checkpoint/correction (724), T4 status/header/density work units (87, 135, 284), and T5 storage/modal/tracking commits (252, 324, 6); native slice assessments may count combined diffs differently. Planned stacked-to-main slices: slice 1 = T1 commits `b7f2e0f6`, `e6908aa9` (213); slice 2 = T2 commit `ac6e208f` (400), dependent on slice 1 merging first. T3's 724-line combined candidate needs a further delivery-size decision before any PR; the 400-line task heuristic did not justify omitting theme validation or tests. No PR created. Branch: `feat/visual-customization`, from main, an isolated feature worktree. The shared main worktree was switched concurrently to `feat/vim-prompt-mode` and must remain untouched. T1 commits `b7f2e0f68ba9782b0ac6a9de9f84fd9808463234` and `e6908aa97cf9792571df66debacc8ad555007cae`; T2 commit `ac6e208f7b128c961d5801e0262986ff4defee71`; T3 checkpoint `a0376266738e557cde83d8df32e9ed1725c24ca7` and correction `0b3ddaae1324324ac414211c98bfb4d8c3c38994`; T4 commits `07954d8f`, `e2b84414`, `cdb58f89`; T5 commits `930fe369`, `4ba2bf37`; runtime crash correction `e142cccd`; compact panel `88207d82`; Commands-style/removal `99cb88c4`; no push, PR or merge. Each task is delegated direct because it requires reading in preparation for writing and multiple non-trivial files; about 400 authored changed lines per task is advisory only, never a reason to compromise tests or clarity.

## Tasks

- [x] **T1 — Persistent visual model and layout policy.** Implemented separate validated global settings for status/header placement, density and section visibility, with defaults, classified read failures and atomic writes. Profiles remain T5 by scope. Route: delegated direct, because source preparation and two nontrivial files. Evidence: RED missing module, GREEN focused 3/3; follow-up RED unreadable-path classification, GREEN focused 6/6; independent verifier focused 4/4 before correction; writer full `pnpm test` 3404 passed, 38 skipped, 0 failed, provider contract and runtime harness passed; parent post-correction focused 6/6 and `git diff --check` passed. Work-unit commit `b7f2e0f68ba9782b0ac6a9de9f84fd9808463234` (179 authored lines). Native committed candidate review approved and acknowledged (lineage `review-a27c03479e46c542`, target `sha256:5a585dcde57f5f21cec23952972a0fd2ee6a387136f36bb42193b31378147b91`, medium tier, reliability lens). Assessment was unassessable due to schema incompatibility, so the independent verifier ran; no downgrade inferred. Documentation tracking commit `e6908aa97cf9792571df66debacc8ad555007cae` (34 authored lines).
- [x] **T2 — Unified customization overlay.** Registered `/gentle:customize` and palette entry with keyboard-driven overlay; current selections refresh from existing animation/banner/theme stores and visual policy, while layout application remains T4. Banner edits reject malformed/unreadable files without changing legacy commands; reset reports partial writes per store rather than claiming transactionality. Route: delegated direct, because UI integration and tests span multiple files. Evidence: RED missing view and RED stale labels/missing theme/tiny height/banner guard/reset failure; GREEN focused 155/155, writer `pnpm test` 3414 passed, 38 skipped, 0 failed, provider contract and runtime harness passed; parent spot check focused 155/155 and `git diff --check` passed. Committed `ac6e208f7b128c961d5801e0262986ff4defee71` (400 authored lines); native high-risk four-lens review approved/acknowledged, lineage `review-737d2fbd6f4893c6`, target `sha256:ea888f9ef30ef6600d7c2e958b660776ae9b9454262f10cde7c516ea74d28a1d`. Advisories R2-001/002, R3-001 and R4-001 are non-blocking and do not reopen that candidate; consider applicable future work only.
- [ ] **T3 — Installed theme selection and source-palette preview only.** The original create/edit/import/export scope was implemented and reviewed in `a0376266`/`0b3ddaae`, but user live testing found the editor confusing and a pasted-HEX issue, then explicitly removed the entire custom-theme editor. Preserve installed-theme selection and modal-only source-color preview; remove reachable custom-theme creation, import and export paths, obsolete UI/tests/docs, and dead write code where safe. Route: delegated direct because extension/view/theme-source/tests/docs span multiple files. Checks: strict TDD for absence of removed actions, installed preview without global theme mutation, explicit selection, focused tests, `pnpm test`, `git diff --check`, native review as due, and live visual confirmation. Evidence: strict TDD RED for still-present custom editor and hidden profile submission, GREEN focused 186/186 and full `pnpm test` 3460 passed, 38 skipped; independent verifier confirmed no reachable theme-editor/write helpers, installed source preview and explicit theme selection. Removed in `99cb88c4ce7737fdecb38d1ecac8cf5a7874b905`, cumulative high-risk native review `review-734bbc6e876df676` approved and acknowledged. **Partial:** final removal and installed theme selection remain to be visually confirmed in live TUI.
- [ ] **T4 — Responsive status and header layout.** Applied right/bottom/hidden presentation; `hidden` removes only the sidebar and retains bottom status like mobile. Moved fullscreen header below input through Pi 0.85.1 `belowEditor` widget independently of sidebar, preserving default top placement. Applied density and all five visibility flags, including independently owned Agents widget, with live updates and reset; comfortable/all-visible remains baseline. Route: delegated direct due renderer/extension/test spread. Evidence: strict TDD RED/GREEN per slice; focused status 179/179, header 145/145, density/visibility 317/317; writer full `pnpm test` last 3447 passed, 38 skipped, provider contract and runtime harness passed; parent focused 317/317 and `git diff --check` passed. Independent verification found a modelId/cwd substring collision, corrected before commit. Commits `07954d8f5f3879ecc4ffb40960def9c4c810863d` (87 lines), `e2b8441463a0ee89d6ee03b09c2d8f4db469f3a5` (135), `cdb58f89fd69fea01f71c48f2f594a2a5f7a799f` (284); native high-risk reviews approved and acknowledged as `review-db1f98f2c7d3851e`, `review-dc1a34f4ef350f12`, and `review-4b1e3ef6f203d42b`. **Partial:** no live interactive fullscreen/regular/narrow TUI visual proof of header and rail. Host-private rail adapter remains version-sensitive; no direct screen clears. Do not check off until live visual behavior is confirmed or limitation accepted.
- [ ] **T5 — Visual profiles and documentation.** Implemented validated versioned named profiles combining theme, banner, animation and visual settings; modal preview/cancel, explicit save/replace/apply/delete/catalog reset and concise README guidance. Applying across stores reports partial results; it is not atomic. Existing commands remain. Route: delegated direct due multiple storage/UI/test/doc files. Evidence: strict TDD RED/GREEN, independent verification found invisible/mutable-target confirmations, corrected before commit; focused 140/140, writer `pnpm test` 3463 passed, 38 skipped, parent focused 140/140, `git diff --check` passed. Work-unit commits `930fe36912d610edd838f4bc1d3834d58e5b3fca` (252 authored lines) and `4ba2bf3749b39f10bc6d40f2b60612f7ba976754` (324 authored lines), plus tracking commit `ecbf5626` (6 lines); cumulative candidate assessment counted 580 changed lines, and native high-risk review approved and acknowledged as `review-791678b586857e92`. **Partial:** live interactive visual proof of profile preview/confirmation not obtained; a bounded isolated offline PTY probe booted Pi but never captured the customization modal. Do not check off until visual behavior is confirmed or limitation accepted.
- [x] **T6 — Correct real Pi theme callback receiver crash.** User's interactive attempt exposed a real Pi 0.85.1 `Theme.fg` receiver loss in `VisualCustomizeView.render` (reading `fgColors` of undefined), hidden by arrow-function mocks. Route: delegated direct for source and regression test. Evidence: strict TDD RED reproduced exact exception with real Pi Theme; GREEN 132/132 focused; full `pnpm test` 3464 passed, 38 skipped; independent spot 132/132 and `git diff --check`. Commit `e142cccd9ee00a29b2614f6082db0f26c1865143` (30 authored lines), medium-risk native assessment `under_budget` for that commit; subsequently included in approved cumulative high-risk review `review-149d8d49b7fa6177`.
- [ ] **T7 — Redesign customization presentation.** User screenshot of live fullscreen settings showed a flat 31-row list spread across the whole terminal, excessive empty space, no visible grouping and hidden previews. User chose compact category navigation beside controls and a read-only visual preview; narrow terminals present one category per screen. Implemented seven explicit categories, 76-column/12-row cap, selectable controls with independent scroll, selected-theme source-palette swatches, truthful static/schematic samples for banner/animation/layout/sections/profiles, and short-height preview omission. Existing actions, persistence, keyboard access, identity-bound confirmations and explicit theme Save remain. Route: delegated direct due view/extension/tests/docs. Evidence: strict TDD RED for flat frames, four usability regressions and missing theme-source preview; GREEN focused 184/184, `pnpm test` 3473 passed, 38 skipped, `git diff --check` passed. Independent verification found and then rechecked confirmation visibility, Tab, short-height rows, false preview and source palette nonmutation; final focused spot 184/184. Work-unit commit `88207d82fd56191f955710d51b6927854c4001d6` (357 authored lines), cumulative native high-risk review `review-149d8d49b7fa6177` approved and acknowledged. **Partial:** user's new fullscreen screenshot shows the compact content still floats as small, frameless text with excessive empty space; visual acceptance failed. T8 addresses this without discarding category/action behavior; the custom theme editor is now removed by T3 scope change.
- [ ] **T8 — Match Commands panel visual style.** The user requested Commands' visual style after seeing the compact panel floating without a frame. The main categories screen and profile subview now share Commands' card frame, padding and selected-row treatment; keep width/height-safe behavior, preview and confirmation guards. The theme-editor subview was briefly adapted, but the user explicitly removed it before this work unit was committed; delete that subview instead of polishing it. Route: delegated direct due view/overlay/tests. Checks: strict TDD RED/GREEN for Commands-style main/profile frames, narrow/short width-safe output, focused tests, full `pnpm test`, `git diff --check`, independent check, native assessment and live visual screenshot. Evidence: `99cb88c4` reuses Commands card and selection renderer; profile hidden-input and resize-without-rerender confirmation regressions went RED/GREEN; final focused 186/186, full `pnpm test` 3460 passed, 38 skipped, `git diff --check` passed. Native review approved/acknowledged `review-734bbc6e876df676`. **Partial:** fresh fullscreen/regular/narrow screenshot of the final UI (main and profiles) remains pending.

## Progress and next step

Exploration complete for existing controls and likely APIs; user confirmed the movable row is the Gentle Shell header. Branch and separate worktree created. Next: the user accepted all other panel behavior, explicitly removed custom-theme create/edit/import/export, and requested a PR and merge. Local removal and Commands-style main/profile checks passed; T3, T4, T5, T7 and T8 remain open for fresh visual confirmation of the final UI. The removed theme-editor bug is no longer reachable, not a claim of repaired editing. Remote PR/merge remains blocked until the user identifies the destination and authorized credential/session, and the required approved issue is established. Automated tests and review did not establish the old editor's usability. The next reviewed boundary is `99cb88c4ce7737fdecb38d1ecac8cf5a7874b905`. Pi 0.85.1 layout geometry and public `belowEditor` widget were exercised in automated tests, not a live TUI. User authorized push and merge in principle, but no remote destination and credential/session were specified; do not perform remote actions until separately authorized. Do not check off a task or claim a supported reposition until behavior is verified. Mirror this full document in Engram under `odd/visual-customization/tasks` and read both back before the first source edit.

## Delivery update — 2026-09-24

This section supersedes earlier delivery strategy and remote-authorization statements, while preserving historical task evidence above. The maintainer explicitly chose a **feature-branch chain** instead of stacked-to-main PRs: only the complete, final Commands-style UI may reach `main`. The historical custom-theme editor and the rejected flat panel must not be merged into `main` as intermediate states. Keep the integration tracker draft/no-merge until all child PRs, checks, and final live TUI visual acceptance complete.

- The verified repository is `Gentleman-Programming/gentle-shell`, base `main`; issue [#1409](https://github.com/Gentleman-Programming/gentle-shell/issues/1409) is `status:approved` after explicit maintainer authorization and target-host readback.
- Remote `main` advanced to `a9232643fde7929a9f780396505bfa1bb5455ea3` during delivery. The first foundation PR [#1410](https://github.com/Gentleman-Programming/gentle-shell/pull/1410) is open against `main` at `6bf3a4a368ec03a0ef1ef66f487c6550536fb209`, 179 changed lines, all reported checks green. **Retarget it to this tracker before merging.** Its review assessment was medium / `under_budget`, no native review due.
- A second local branch `feat/visual-customization-panel` contains a staged 400-line command/control/view slice plus real Pi Theme regression and scoped README; focused 149/149 and full `pnpm test` 3,228 passed, 43 skipped. It deliberately remains uncommitted/unpublished while the chain changes topology; this flat UI is a reviewable intermediate child only, never a direct-main deliverable.
- The final feature diff on the prior main base had 2,345 authored changed lines across 23 files. An earlier eight-slice allocation was unmeasured and is not a size certificate. Measure every child against its immediate parent; keep tests/docs with behavior. After one honest slicing pass, request maintainer `size:exception` with rationale for any inseparable >400-line child rather than compressing code or tests.
- Outstanding: retarget #1410 to the tracker, prepare/publish child slices that produce the final installed-theme-only Commands-style panel and profiles, run independent checks and final live fullscreen/regular/narrow visual acceptance, then merge children into the tracker and the tracker into `main`. No feature PR has been merged.

### Child PR checkpoint — 2026-09-24

- Tracker [#1411](https://github.com/Gentleman-Programming/gentle-shell/pull/1411) is open **draft**, base `main`, issue #1409 approved, `type:feature`. Current tree includes this tracking document only; it must not merge until final integrated acceptance.
- Foundation [#1410](https://github.com/Gentleman-Programming/gentle-shell/pull/1410) was retargeted to the tracker without rewriting remote history. Its current branch head `76e52741` includes tracker ancestry; the PR diff remains exactly 2 files and 179 lines. Checks previously passed on the old base; verify the new-base checks before integration.
- Intermediate panel [#1412](https://github.com/Gentleman-Programming/gentle-shell/pull/1412) is open against #1410's branch, exactly 8 files and 400 lines at branch head `d7d4159f`. Its work-unit commit is `6a6d1367`; after doc-only tracker alignment, focused checks passed 149/149 and `git diff --check` was clean. The prior full `pnpm test` passed 3,228 with 43 skipped; native four-lens high-risk review `review-55a7269181f14d83` approved and acknowledged. GitHub CI remains pending. This intermediate flat view must be superseded by final Commands-style behavior before the tracker can merge to `main`.
- Next: measure and implement remaining cohesive child slices for installed themes, Commands-style presentation, responsive layout, and profiles; run their tests and native checks, confirm final live TUI appearance and interaction, then integrate the approved chain and merge only the tracker into `main`. No feature PR has been merged.

## Single-PR consolidation — 2026-09-24

This section supersedes the chain delivery plan above. The user explicitly requested **one final PR with `size:exception`**: reuse draft [#1411](https://github.com/Gentleman-Programming/gentle-shell/pull/1411) as the sole PR to `main`, rather than publish more children. #1410 and #1412 are historical reviewed intermediate branches; do not merge either. Close them as superseded only after the final PR contains and verifies their source. The user said not to wait for CodeRabbit; required checks, strict TDD, typecheck, native review when due, and final live TUI acceptance remain in force. No admin bypass of the final PR's required checks was authorized.

The integration worktree at commit `5e6df9510ab908f8bb2c53c94c5e0c024eeac53b` now contains the reviewed foundation, corrected intermediate panel, and installed-theme preview work unit `041beff550fb594fb91b71e46df05b7ea0beda97`. Its cumulative diff against base `a9232643` is 13 files, 773 additions and 10 deletions including this task document before this update. The corrected panel's 401-line review `review-57f6d17ae2747eb6` and the installed-theme work unit's 165-line review `review-e57f9b8ad001b042` were both approved and acknowledged. Local installed-theme focused tests passed 121/121; full `pnpm test` passed 3,243 with 34 skipped; `pnpm run typecheck` passed with no new diagnostics. These are work-unit results, **not yet integrated final PR or live TUI proof**.

- [x] **T9 — Consolidate reviewed base and installed themes in the single PR.** Route: delegated direct for source work; the Git integration merge is `5e6df951`, followed by task-plan commit `81a0e192`. The integrated head `81a0e192` passed `pnpm run typecheck` (188 recorded baseline diagnostics, no regressions), focused Node tests (127 passed, 0 failed), `pnpm test` (3,243 passed, 34 Windows-native skipped, 0 failed), and `git diff --check`; the tracked tree remained clean. Installed-theme highlight previews without activation, explicit Enter/Space applies, and no custom-theme create/edit/import/export control is reachable. Native committed-range assessment from `af052f15` reported high risk (741 changed lines); four-lens review `review-92d462421bd5b4c2` approved and was acknowledged for exact head `81a0e192`, with four non-blocking advisory findings. The reviewed source was pushed to draft PR #1411 via guarded HTTPS; its current `main` diff is 785 additions and 10 deletions across 13 files. Later source or task-record commits need their own candidate scope rather than inheriting this receipt.
- [x] **T10 — Port responsive layout and visibility.** Route: delegated direct across extensions, sidebar/bar adapters, tests and README. Commit `5d8dc3953099a2d30d480f67e28ef78ae6e01faf` (263 authored changed lines/10 files) implements right/bottom/hidden placement with responsive 140-column default; hidden suppresses the rail but preserves bottom Status, and the fullscreen header uses Pi's `belowEditor` widget independently of the rail. Comfortable/compact/minimal density and section visibility update live for Changes, Agents, TODO, usage/cost and model details. Strict TDD RED reproduced hidden/status cases; independent verifier found a possible stale-ownership transition; a further RED test observed `[]` instead of `["Status"]` before root recomputation, then GREEN after a live placement/breakpoint guard. Focused six-file suite passed 317/317; `pnpm test` passed 3,249 with 34 Windows-native skips, provider contract/runtime harness passed; `pnpm run typecheck` had 188 recorded baseline diagnostics and no regressions; `git diff --check` and parent 34-test layout spot check passed. Native committed-range assessment from `f0457876` was high risk; four-lens `review-52a7fa6d371cfa30` approved and was acknowledged, with two non-blocking advisory findings. No terminal clears or custom-theme editor paths were added. Live fullscreen/regular/narrow TUI acceptance remains **pending under T13**, not inferred from tests.
- [x] **T11 — Port named visual profiles.** Route: delegated direct across versioned storage, modal, extension, tests and README. Work-unit `9bfc4ec8bcdff5e92c2ce10071b15b45ad2a15bd` changes 8 files/693 authored lines: validated named catalog with atomic writes; explicit save/replace/apply/delete/reset; identity- and visibility-bound confirmation; installed-theme-only guard; honest PARTIAL cross-store apply. The cohesive behavior needs storage, integration, regression tests and docs together, so the advisory 400-line task heuristic did not justify cutting proof or compressing code. Strict TDD RED observed unavailable-theme save, malformed/unreadable animation save, zero-width confirmation/input, and symlinked catalog before fixes; GREEN after guards; storage and tests reformatted before final verification. Focused five-file suite 140/140; final `pnpm test` 3,261 passed, 34 Windows-native skipped, provider contract/runtime harness passed; `pnpm run typecheck` retained 188 baseline diagnostics with no regressions; `git diff --check` passed. One earlier full-suite run failed an unrelated Agents timing test under load; its file passed 120/120 alone, and **one** full rerun passed—both observations retained, not treated as a silently passing first run. Independent read-only verification passed the final storage safeguards; Windows has a documented residual lstat/open race where `O_NOFOLLOW` is unavailable, while stable symlinks are rejected and the symlink test skips only when creation lacks permission. Native assessment of `3e296af1..9bfc4ec8` was high risk, but exact preflight STATUS returned `rdd_disabled`; mode status confirmed clone-local off, so no review was started and delivery is **disabled/unmanaged**, not natively approved. Live fullscreen/regular/narrow Profiles acceptance remains pending under T13.
- [x] **T12 — Deliver final Commands-style main and Profiles panels.** Route: delegated direct across shared Commands card/selection renderer, customization view and extension, with focused regressions. Work-unit `e63af0788af145eb6946239cd6f22a65b06775dd` changed 5 files/855 authored lines: category-pane/controls-pane frame and selected-row treatment, truthful read-only previews, installed-theme-only selection, Profiles subview, safe narrow/short behavior and profile-name input. No reachable custom-theme editor/create/import/export path. The 400-line task heuristic remained advisory: shared rendering extraction, cohesive view rewrite and tests belong together; the user approved one PR with `size:exception`. Strict TDD RED observed frameless view, missing category navigation and unsafe narrow/short behavior, then GREEN. Independent verifier found two further blockers despite 179/179 passing: invisible control activation at unusable widths and same-name profile content drift at confirmation. Bounded writer observed RED for both, fixed width/height visibility and content-bound apply/replace/delete confirmation, then GREEN. Final focused five-file suite 181/181; `pnpm test` 3,274 passed/34 Windows-native skipped, provider contract/runtime harness passed; `pnpm run typecheck` retained 188 baseline diagnostics without regressions; `git diff --check` clean. Independent read-only recheck found both blockers resolved and reran 181/181; parent repeated typecheck and diff check. Clone-local RDD remained off after the commit; no native review was started and delivery is `disabled/unmanaged`, not natively approved. Live TUI acceptance remains T13.
- [ ] **T13 — Final acceptance and single-PR delivery.** The first live isolated Pi 0.87.1 probe failed to register the command; explicit `GENTLE_PI_SHELL=1 GENTLE_PI_AGENTS_CHILD=0` enabled it in a later session. Actual TUI captures at 160×42 showed the Commands frame, selected row, right Status rail and current dark theme unchanged while highlighting installed Gentle changed only the source-palette preview. At 100×30 the main and Profiles frames and bottom Status rendered; 60×20 stayed width-safe and 40×20 switched to one-category controls. A disposable profile snapshot showed dark/quality/pink and the saved layout even after live changes; apply/replace/delete each prompted for that profile and Escape cancelled without deleting it. Live right/bottom/hidden modes at 160 and bottom Status at 100/60, plus below-input header at all three sizes, were observed and restored in a throwaway HOME. That session exposed an actual top-header omission at 100/60, because it was coupled to the >=140-column rail. Strict TDD RED (two header assertions) then GREEN corrected it in work-unit `b6936f786cb7c64efd4fa4debe9b010245f3dd96` (90 authored lines/2 files); focused four-file suite 180/180, full `pnpm test` 3,277 passed/34 Windows-native skipped with provider contract/runtime harness passed, `pnpm run typecheck` 188 baseline diagnostics/no regressions, `git diff --check` clean. Independent read-only verification found no blocker and repeated 180/180. A fresh Pi live capture of the corrected bytes confirmed top header at 160/100/60, below-input at 100/60, restoration to top without duplicate, right rail at 160 and bottom Status at 100/60 (evidence `/tmp/gentle-pi-tui-accept.GCExEQ/`); no model prompts or source changes in those sessions. Native RDD remains clone-local off, so no review was started or approval claimed (`disabled/unmanaged`). **Pending:** re-run full/focused checks on the exact final tracked head after this record, confirm required GitHub CI on that head, remeasure and update #1411 with honest `size:exception`, close #1410/#1412 as superseded only after source confirmation, mark #1411 ready and merge only when the final checks pass; do not wait for CodeRabbit or waive a failed required check.

### Main integration checkpoint — 2026-09-24

Remote `main` advanced through Vim PR #1415 to `4b2d21d93da8271e99d8bf7b14edbd0a7cf62a3f`, making #1411 conflict in `extensions/gentle-shell.ts`. The isolated integration branch fetched `main` over guarded HTTPS and merged it at `0ea1a8efa4cd907ee29afbdac6af6eb95041141e` (second parent `4b2d21d9`), without modifying the separate Vim worktree. The conflict resolution retained both `/gentle:vim` and `/gentle:customize`, Vim policy/listeners and visual profiles/layout; the palette lists both commands exactly once. Focused six-file suite 329/329, full `pnpm test` 3,423 passed/34 platform skips, `pnpm run typecheck` 188 baseline/no regressions and staged diff check passed. Independent read-only verification compared both parents and reran 329/329; no integration blocker. Clone-local RDD remained off (`disabled/unmanaged`, no native approval). A merged-head live offline Pi TUI run in a disposable HOME confirmed both commands in completion, the literal `/gentle:customize` editor buffer before Enter, Commands-style frames and selected row, installed-theme preview without activation, named-profile confirmation/cancel, and header top↔below-input with right/bottom status at 160/100/60 columns. An earlier merged-head submission produced a model-key error without a pre-Enter buffer capture; a later controlled literal submission opened the panel; the first error's cause remains unproven. Evidence: `/tmp/pi-visual-merged-0ea1a8ef.retry.log` and `.commands-ansi.log`. **Pending:** commit this checkpoint, verify full/focused/typecheck on the exact tracked head, push and obtain green required GitHub CI for that head, measure/revise PR size rationale, close superseded children, synchronize the complete Engram mirror, and merge #1411 only when all final gates pass.

Mirror status: **pending**. The complete Engram observation `odd/visual-customization/tasks` (#20396) still reflects T9; the T10 mirror worker had no Engram read/write tools and made no update. This local file (including the T10–T13 and main-integration evidence recorded after `08d8e8bc`) is authoritative until the full-document mirror can be upserted and read back. Do not infer T10–T13 absence from the stale observation.
